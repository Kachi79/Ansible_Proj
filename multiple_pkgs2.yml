## This playbook installs multiple pkgs at the same time using the [] function.
---
- name: Installing packages in ansible 
  hosts: RHEL 
  become: true
  tasks:
  - name: installing packages 
    dnf:
      name: ['tree', 'httpd', 'httpd']
      state: present 
  